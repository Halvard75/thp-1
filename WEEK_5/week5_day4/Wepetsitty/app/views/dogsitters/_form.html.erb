<%= form_with(model: dogsitter, local: true) do |form| %>
  <% if dogsitter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(dogsitter.errors.count, "error") %> prohibited this dogsitter from being saved:</h2>

      <ul>
      <% dogsitter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :first_name %>
    <%= form.text_field :first_name, id: :dogsitter_first_name %>
  </div>

  <div class="field">
    <%= form.label :last_name %>
    <%= form.text_field :last_name, id: :dogsitter_last_name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
