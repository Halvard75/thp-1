<%= form_with(model: stroll, local: true) do |form| %>
  <% if stroll.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(stroll.errors.count, "error") %> prohibited this stroll from being saved:</h2>

      <ul>
      <% stroll.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, id: :stroll_date %>
  </div>

    <div class="field">
    <%= form.label :Dogsitter %>
    <%= form.text_field :dogsitter_id, id: :dogsitter_id %>
  </div>

    <div class="field">
    <%= form.label :Dog %>
    <%= form.text_field :dog_id, id: :dog_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
