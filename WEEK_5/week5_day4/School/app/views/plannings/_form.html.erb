<%= form_with(model: planning, local: true) do |form| %>
  <% if planning.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(planning.errors.count, "error") %> prohibited this planning from being saved:</h2>

      <ul>
      <% planning.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :date %>
    <%= form.text_field :date, id: :planning_date %>
  </div>

    <div class="field">
    <%= form.label :student %>
    <%= form.text_field :student_id, id: :student_id %>
  </div>

    <div class="field">
    <%= form.label :classe %>
    <%= form.text_field :classe_id, id: :classe_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

