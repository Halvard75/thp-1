<%= form_with(model: flight, local: true) do |form| %>
  <% if flight.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</h2>

      <ul>
      <% flight.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :flight_code %>
    <%= form.text_field :flight_code, id: :flight_flight_code %>
  </div>

  <div class="field">
    <%= form.label :duration %>
    <%= form.number_field :duration, id: :flight_duration %>
  </div>

  <div class="field">
    <%= form.label :departure_date %>
    <%= form.datetime_select :departure_date, id: :flight_departure_date %>
  </div>

  <div class="field">
    <%= form.label :departure_airport %>
    <%= form.text_field :destination_id, id: :flight_departure_airport %>
  </div>

  <div class="field">
    <%= form.label :arrival_airport %>
    <%= form.text_field :origin_id, id: :flight_arrival_airport %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
